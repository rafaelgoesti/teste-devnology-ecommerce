name: devnology_ecommerce
description: "🛍️ Devnology E-commerce - Aplicativo móvel Flutter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP requests
  http: ^1.1.0
  
  # State management
  provider: ^6.1.1
  
  # UI components
  cupertino_icons: ^1.0.6
  
  # Cached network image
  cached_network_image: ^3.3.0
  
  # Icons
  font_awesome_flutter: ^10.6.0
  
  # Animations
  lottie: ^2.7.0
  
  # Shared preferences
  shared_preferences: ^2.2.2
  
  # URL launcher
  url_launcher: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
